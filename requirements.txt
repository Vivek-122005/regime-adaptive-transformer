# Pinned to versions known compatible with Phase 3 (Chronos-T5-Small + LoRA r=8)
# Tested under Python 3.11 on macOS arm64 / Linux x86_64.
# See data/manifest.csv for the data fingerprint expected by these pins.

# Core
numpy==1.26.4
pandas==2.2.2
yfinance==0.2.40
pyarrow==15.0.2

# ML / baselines
hmmlearn==0.3.2
scikit-learn==1.4.2
xgboost==2.0.3
lightgbm==4.3.0

# DL stack (Phase 2 RAMT + Phase 3 Chronos-LoRA)
torch==2.2.2
transformers==4.40.2
peft==0.10.0
accelerate==0.29.3
datasets==2.18.0

# EDA / notebooks
matplotlib==3.8.4
seaborn==0.13.2
scipy==1.13.0
statsmodels==0.14.2
jupyter==1.0.0
ipykernel==6.29.4

# Utils
tqdm==4.66.4
PyYAML==6.0.1

# Dashboard
streamlit==1.34.0
plotly==5.21.0
requests==2.31.0
beautifulsoup4==4.12.3
